# Global NGO Impact Summit — Premium Website
Upload these files to the root of your GitHub repository and enable GitHub Pages from `main` / root.

## Before publishing
- Replace `YOUR_FORM_ID` in `index.html` with the real Google Forms URL.
- Verify the USDT/BEP20 address and exact application fee.
- Confirm the official venue, award terms, eligibility, selection and refund policy.
- Images are remote Unsplash assets; replace with properly licensed/local images if desired.
