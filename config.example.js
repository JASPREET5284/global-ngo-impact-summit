// Copy this value into script.js.
// Never invent or guess a Binance address.
// Also publish the exact network next to the address.
const CONFIG = {
  binanceAddress: "PASTE_VERIFIED_BINANCE_ADDRESS_HERE"
};
